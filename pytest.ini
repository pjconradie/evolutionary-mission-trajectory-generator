[pytest]
testpaths = tests
addopts = -ra
markers =
	unit: isolated tests without external services
	integration: tests that exercise external tools or combined components
	regression: tests that preserve established EMTG behavior and baselines
